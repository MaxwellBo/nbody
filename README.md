## Serial

- `./run.sh` [filename from `./inputs`]

e.g. `./run.sh in_8_body_counterclockwise`


## Parallel

- `module load mpi` or `module load openmpi-1.8-x86_64` or `module load gnu/6.3.0`
- `make`
- `sbatch go.sh`

